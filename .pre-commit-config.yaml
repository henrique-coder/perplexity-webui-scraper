repos:
  - repo: local
    hooks:
      - id: lint
        name: project lint (pre-push)
        entry: just lint
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
