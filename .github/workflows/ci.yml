name: CI

on:
  pull_request:
    branches:
      - prod
  push:
    branches:
      - prod

jobs:
  lint_and_test:
    name: Code Quality & Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Install Dependencies
        run: just install

      - name: Run Linter (Ruff, Ty, Prettier, Taplo)
        run: |
          uv run ruff format --check
          just lint

      - name: Run Tests (Pytest)
        run: just test
